# PROJECT_BRAIN

## 1. Project Overview

- **Что это:** Одностраничное web-приложение для создания задач в Bitrix24 через REST API (входящий вебхук)
- **Задача:** Упростить и стандартизировать постановку задач в команде — выбрать разработчика, постановщика, проект, дедлайн, заполнить структурированное описание, прикрепить файлы, добавить EOD-комментарий от бота — всё в одном клике
- **Стек технологий:**
  - HTML5 (один файл `index.html`, ~1569 строк)
  - CSS3 (Custom Properties, Grid, Flexbox, backdrop-filter, keyframes)
  - Vanilla JS (ES6+, async/await, Fetch API, FileReader API, XHR)
  - Google Fonts: JetBrains Mono + Unbounded
  - Inline SVG иконки (без библиотек)
  - LocalStorage для конфигурации и черновиков
  - Bitrix24 REST API (два входящих вебхука: пользователь + бот)
- **Репозиторий:** `https://github.com/Antisakrum2004/bitrix-form`
- **Деплой:** GitHub Pages — `https://antisakrum2004.github.io/bitrix-form/`
- **Текущая версия:** v7.9

## 2. Architecture

### Общая схема

```
┌────────────────────────────────────────────────────────┐
│  Браузер (index.html)                                  │
│  ┌──────────┐  ┌──────────┐  ┌─────────────────────┐  │
│  │  UI Form │→ │  JS App  │→ │ Bitrix24 REST API   │  │
│  │ (CSS/    │  │ (Vanilla │  │ ┌─────┐ ┌────────┐  │  │
│  │  HTML)   │  │  async)  │  │ │Hook │ │EOD Bot │  │  │
│  │          │  │          │  │ │ 116 │ │ Hook   │  │  │
│  │          │  │          │  │ │(user│ │ 154    │  │  │
│  │          │  │          │  │ │ +ev)│ │(bot)   │  │  │
│  └──────────┘  └──────────┘  │ └─────┘ └────────┘  │  │
│                     ↕        └─────────────────────┘  │
│              LocalStorage                               │
│       (вебхук, черновик, история задач)                │
└────────────────────────────────────────────────────────┘
```

- **Backend:** Отсутствует. Прямые вызовы Bitrix24 REST API из браузера
- **Frontend:** Единый HTML-файл со встроенными CSS и JS
- **Интеграция:** Bitrix24 REST API через два входящих вебхука:
  - **Хук пользователя (116):** создание задач, файлы, наблюдатели — все основные операции
  - **Хук бота (154):** только EOD-комментарии — чтобы комментарии приходили от имени «Айти Лаб Bot»

### Ключевые зависимости

- `Google Fonts CDN` — шрифты (JetBrains Mono, Unbounded)
- `Bitrix24 REST API` — все данные и операции
- Внешних npm-пакетов нет

### Как идут данные

1. Пользователь заполняет форму → JS собирает payload
2. `POST {user-hook}/tasks.task.add` → создание задачи, получение `task.id`
3. `POST {user-hook}/task.observers.add` (до 4 вариантов эндпоинта) → добавление наблюдателя
4. Если EOD-галка стоит: `POST {user-hook}/addObserver` с userId=154 → бот становится наблюдателем
5. Если EOD-галка стоит: `POST {bot-hook}/task.commentitem.add` → EOD-комментарий от бота
6. `GET {user-hook}/disk.folder.uploadfile?id=3` → получение upload URL
7. Загрузка файла на upload URL (FormData метод)
8. `POST {user-hook}/tasks.task.update` с `UF_TASK_WEBDAV_FILES: ['n'+diskId]` → привязка файла к задаче
9. Сброс формы, показ toast

## 3. Core Modules

### Модуль: UI / Дизайн-система
- **Путь:** `index.html` → `<style>`
- **Ответственность:** Glass-морфизм, тёмная тема, CSS-переменные, анимации, адаптивность
- **Связи:** Используется всеми UI-компонентами через CSS-классы

### Модуль: Topbar + Settings
- **Путь:** `index.html` → `.topbar`, `.settings-panel`
- **Ответственность:** Логотип, бейдж версии, индикатор черновика, кнопка настроек, панель ввода вебхука
- **JS-функции:** `oS()`, `cS()`, `mWS()`, `sWH()`
- **Связи:** LocalStorage (`bx_webhook`)

### Модуль: Sidebar (Гайд)
- **Путь:** `index.html` → `aside.sidebar`
- **Ответственность:** Инструкция по заполнению формы, таблица маппинга полей → API
- **Связи:** Независим, скрывается на мобильных

### Модуль: Шаблоны задач
- **Путь:** `index.html` → секция шаблонов
- **Ответственность:** Предустановленные шаблоны описания задач (Bug, Feature, Hotfix и т.д.)
- **JS-функции:** `applyTemplate()`, `clearTemplate()`, `_lockScroll()`, `_unlockScroll()`
- **Связи:** Заполняют поля описания, скролл-лок для мобильных

### Модуль: Карточка 1 — Название
- **Путь:** `.card` Card 1
- **Ответственность:** Поле TITLE, огонёк важности (PRIORITY), кнопка «Повторить»
- **JS-функции:** `tF()` (toggle flame), `rL()` (repeat last), `val()` (валидация)
- **Связи:** LocalStorage (`bx_last_task`)

### Модуль: Карточка 2 — Команда
- **Путь:** `.card` Card 2
- **Ответственность:** Выбор Разработчика (RESPONSIBLE_ID), Постановщика (CREATED_BY), Наблюдателя, EOD-галка
- **JS-функции:** `renderChips()`, `sC()` (select chip)
- **Данные:** Жёстко закодированные массивы `DEVS`, `MGRS`, `OBSS` с Bitrix24 user ID
- **EOD:** Чекбокс «EOD» в заголовке карточки, выровнен вправо, включён по умолчанию
- **Связи:** API-запрос на добавление наблюдателя, EOD-комментарий от бота

### Модуль: Карточка 3 — Сроки
- **Путь:** `.card` Card 3
- **Ответственность:** Дата дедлайна + быстрые кнопки (+3д, +7д, +14д)
- **Особенность:** Дедлайн всегда инициализируется текущей датой (локальное время, не UTC)
- **Связи:** Поле DEADLINE в payload

### Модуль: Карточка 4 — Описание
- **Путь:** `.card` Card 4
- **Ответственность:** Структурированное описание задачи — секции:
  - Цель (textarea)
  - Что сделать (динамические поля, до 10)
  - Критерии приёмки (динамические поля, до 10)
  - Контакт (textarea)
  - Материалы (textarea)
  - Файлы (drag & drop + file input + clipboard paste Ctrl+V)
  - Предпросмотр описания (живая сборка)
- **JS-функции:** `bD()` (build description), `up()` (update preview), `ufi()`, `initDynamicFields()`, `addDynamicField()`, `removeDynamicField()`, `resetDynamicFields()`, `getDynamicValues()`, `ag()` (auto-grow textarea)
- **Связи:** `UF_TASK_WEBDAV_FILES`, `tasks.task.update`

### Модуль: Карточка 5 — Проект
- **Путь:** `.card` Card 5
- **Ответственность:** Выбор проекта (GROUP_ID)
- **JS-функции:** `renderProjs()`, `sP()`
- **Данные:** Жёстко закодированный массив `PROJS` с проектами и их Bitrix24 group ID

### Модуль: API-интеграция (создание задачи)
- **Путь:** `index.html` → `<script>` функция `handleSubmit()`
- **Ответственность:**
  - Создание задачи через `tasks.task.add`
  - Добавление наблюдателя через 4 варианта эндпоинта с фоллбэком
  - Добавление бота (154) как наблюдателя (для прав на EOD-комментарий)
  - EOD-комментарий через хук бота (154)
  - Загрузка файлов: FormData метод
  - Защита от двойного клика (`isSubmitting` flag)
  - Проверка на дубликат задачи (5 мин)
- **Связи:** Bitrix24 REST API (2 вебхука), LocalStorage

### Модуль: EOD-комментарий
- **Путь:** `index.html` → функция `addEodComment(hook, tid, projId)`
- **Ответственность:** Отправка шаблона EOD (End of Day) как комментарий к задаче от имени бота
- **Формат:** BBCode (`[b]`, `[i]`, не HTML)
- **Шаблон:**
  ```
  [i]Заполни EOD при срезе задачи, если она решается более 4х часов и по завершении задачи:[/i]
  [i]Стадия тестирования обязательна при завершении задачи[/i]
  [i]Не забываем прикладывать инструкцию по завершению задачи[/i]  ← только для проекта Dakota (ID=32)

  [b]Done[/b]:
  [b]Test[/b]:
  [b]Next[/b]:
  [b]Block[/b]:
  ```
- **Условие:** Бот (154) должен быть наблюдателем задачи, иначе нет прав на комментарий
- **Вебхук бота:** `https://1c-cms.bitrix24.ru/rest/154/f896em13hhazm006/`
- **Связи:** Вызывается из `handleSubmit()` если EOD-галка стоит

### Модуль: Загрузка файлов
- **Путь:** `index.html` → функция `uploadFiles(hook, tid)`
- **Ответственность:** Загрузка файлов на диск Bitrix24 и привязка к задаче
- **Метод:** Только FormData (единственный рабочий метод). Raw binary был убран в v7.7 — он «сжигал» upload URL токен
- **Имена clipboard-файлов:** Уникальные с timestamp (`clipboard-1716374400000.png`)
- **Связи:** `disk.folder.uploadfile`, `tasks.task.update`

### Модуль: Черновик + beforeunload
- **Путь:** `index.html` → `saveDraft()`, `clearDraft()`, `loadDraft()` (закомментирован вызов)
- **Ответственность:**
  - `beforeunload` предупреждение если форма заполнена
  - `saveDraft()` — автосохранение в LocalStorage
  - `loadDraft()` — восстановление (закомментировано в DOMContentLoaded)
  - Форма всегда сбрасывается при перезагрузке страницы
- **Связи:** LocalStorage

### Модуль: История задач
- **Путь:** `index.html` → `pushHistory()`, рендер истории
- **Ответственность:** Отображение недавно созданных задач (ID, название, разработчик, проект)
- **Связи:** LocalStorage

### Модуль: Toast-уведомления
- **Путь:** `index.html` → `.toast` + `showToast()`
- **Ответственность:** Success/error/warn уведомления, автоскрытие через 6 секунд
- **Связи:** Вызывается из всех модулей

## 4. Current State

### Что уже реализовано
- Полная форма создания задач с 5 карточками + шаблоны
- Создание задач через Bitrix24 REST API (`tasks.task.add`)
- Выбор разработчика, постановщика, наблюдателя
- Выбор проекта
- Динамические поля «Что сделать» и «Критерии приёмки» (до 10 полей)
- Поле «Контакт»
- Drag & drop + выбор файлов + вставка из буфера обмена (Ctrl+V) для загрузки
- Огонёк важности (4-слойная SVG-анимация) → PRIORITY 0/2
- Быстрые кнопки дедлайна (+3д, +7д, +14д) — локальное время, не UTC
- Дедлайн всегда сегодня при загрузке формы
- Предпросмотр описания (живая сборка)
- Сохранение вебхука в LocalStorage
- Кнопка «Повторить» — восстановление прошлой задачи
- Встроенный гайд в sidebar с таблицей маппинга полей
- Адаптивный дизайн (мобильная версия без sidebar)
- Toast-уведомления
- Авто-рост textarea по содержимому
- Валидация обязательных полей (название, разработчик, постановщик, проект)
- Шаблоны задач (Bug, Feature, Hotfix и т.д.)
- Защита от двойного клика на кнопку создания
- Проверка на дубликат задачи (5 мин)
- **EOD-комментарий от бота** — BBCode-шаблон, хук бота (154), бот добавляется наблюдателем автоматически
- **beforeunload предупреждение** — если форма заполнена
- **Форма сбрасывается при перезагрузке** — loadDraft() закомментирован
- **Индикатор черновика** — мигающий «● черновик» в topbar
- **Скролл-лок на мобильных** — `_lockScroll()`/`_unlockScroll()` при выборе шаблона
- **Загрузка изображений из буфера** — уникальные имена файлов с timestamp
- **История созданных задач**

### Что работает стабильно
- Создание задач через API
- Сохранение/чтение вебхука из LocalStorage
- UI-рендеринг и анимации
- Выбор участников и проектов
- Динамические поля описания
- Повтор последней задачи
- Загрузка файлов (FormData метод)
- EOD-комментарий от бота (154)
- Дедлайн — всегда сегодня, локальное время
- Шаблоны с скролл-локом

### Что частично
- Добавление наблюдателя — 4 варианта эндпоинта с перебором, `tasks.task.update` с AUDITORS — единственный рабочий, остальные возвращают 404 (косметические ошибки в консоли)
- Бот (154) добавляется наблюдателем для EOD — если addObserver упадёт, EOD не сможет комментировать

## 5. Known Issues & Problems

- **CORS-ограничения:** Прямые `fetch()` к Bitrix24 API из `file://` не работают. Требуется запуск через локальный сервер или GitHub Pages
- **Наблюдатель — нет единого рабочего эндпоинта:** `task.observers.add` и `tasks.task.observers.add` возвращают 404. Фоллбэк через `tasks.task.update` с `AUDITORS` работает. Первые 2 попытки — косметический шум в консоли
- **Жёстко закодированные ID:** Массивы `DEVS`, `MGRS`, `OBSS`, `PROJS` содержат конкретные Bitrix24 user/group ID. При изменении состава команды/проектов — правка кода
- **Один файл:** Весь проект в одном `index.html` (~1569 строк). Сложно поддерживать и тестировать
- **EOD зависит от наблюдателя:** Бот должен быть наблюдателем задачи, иначе `task.commentitem.add` от хука бота вернёт «Недостаточно прав»
- **Хук бота захардкожен:** EOD-хук (`/rest/154/f896em13hhazm006/`) вшит в код, не настраивается через UI. Если перевыпустить вебхук бота — нужно править код
- **GitHub Pages build завис на старых версиях:** Был битый submodule `bitrix-form` без `.gitmodules` — ломал Actions. Удалён в v7.9. Также мусорные `.json` файлы с chmod 755 ломали билд — удалены

## 6. Version History (v6 → v7.9)

### v7.0 — Шаблоны задач
- Добавлены шаблоны описания (Bug, Feature, Hotfix и т.д.)
- Клик по активному шаблону сбрасывает форму

### v7.1 — Исправления строк + beforeunload
- Починены сломанные строковые литералы в шаблонах
- Добавлено beforeunload предупреждение

### v7.2 — Дедлайн
- **Баг:** Дедлайн показывал 30.04 вместо сегодняшней даты
- **Причина:** `loadDraft()` восстанавливал старую дату из localStorage ПОСЛЕ установки «сегодня» + условие `if (!dlEl.value)` не давало перезаписать
- **Фикс:** Дедлайн инициализируется ПОСЛЕ loadDraft(), условие убрано, loadDraft не восстанавливает deadline
- **Фикс:** `toISOString()` давал UTC дату — заменено на локальные методы `getFullYear/getMonth/getDate`
- Те же фиксы для quick-dl кнопок (+3д, +7д, +14д)

### v7.3 — Сброс формы при перезагрузке
- **Запрос:** Форма должна обнуляться при перезагрузке, но beforeunload предупреждение оставить
- **Фикс:** `loadDraft()` вызов закомментирован в DOMContentLoaded
- Черновик всё ещё сохраняется (для beforeunload), но никогда не восстанавливается

### v7.4 — Мобильный скролл (попытка 1)
- **Баг:** На мобильных при выборе шаблона страницу кидает вниз
- **Причина:** `autoGrow()` на текстареях заставляет браузер скроллить
- **Попытка:** Двойной `requestAnimationFrame` вместо `window.scrollTo`
- **Результат:** Не помогло

### v7.5 — Мобильный скролл (попытка 2) ✅
- **Фикс:** Скролл-лок через `position:fixed` + `top:-scrollY` на body
- `_lockScroll()` — фиксирует позицию, `_unlockScroll(y)` — возвращает через 50мс
- **Результат:** Работает

### v7.6 — EOD-галка + загрузка изображений
- Добавлена EOD-галка в заголовок карточки «2 Команда» (выровнена вправо, включена по умолчанию)
- EOD не зависит от шаблонов
- `addEodComment()` — отправка комментария через `task.commentitem.add`
- Интегрировано в `handleSubmit()`

### v7.7 — Фикс изображений + BBCode ✅
- **Баг:** Изображения из буфера не загружались в Bitrix
- **Причина:** Method A (raw binary) «сжигал» upload URL токен с ошибкой `DISK_BASE_SERVICE_22001`, Methods B/C получали `DISK_OBJ_22000` (токен уже использован)
- **Фикс:** Method A удалён, оставлен только FormData метод
- **Фикс:** Clipboard-файлы получают уникальные имена с timestamp
- **Фикс:** EOD форматирование — HTML `<b>`/`<i>` заменено на BBCode `[b]`/`[i]`
- **Фикс:** Dakota-строка перенесена наверх к другим italic-строкам

### v7.8 — AUTHOR_ID попытка
- Добавлен `AUTHOR_ID: '154'` в EOD-комментарий
- Обновлён текст EOD на новый формат
- **Результат:** Не сработало — вебхук от пользователя 116, Bitrix24 игнорирует AUTHOR_ID, подставляя владельца вебхука

### v7.9 — EOD через хук бота ✅
- **Решение:** Создан отдельный входящий вебхук от пользователя 154 (Айти Лаб Bot)
- `EOD_HOOK = 'https://1c-cms.bitrix24.ru/rest/154/f896em13hhazm006/'` — вшит в код
- Перед EOD-комментарием бот (154) добавляется как наблюдатель через `addObserver()`
- EOD-комментарий отправляется через хук бота → отображается от имени «Айти Лаб Bot»
- AUTHOR_ID убран (не нужен — хук и так от бота)
- Обновлён текст EOD:
  - `[i]Заполни EOD при срезе задачи, если она решается более 4х часов и по завершении задачи:[/i]`
  - `[i]Стадия тестирования обязательна при завершении задачи[/i]`
  - Для Dakota (ID=32): `[i]Не забываем прикладывать инструкцию по завершению задачи[/i]`
- Починен GitHub Pages deploy — удалён битый submodule `bitrix-form`, мусорные `.json` файлы
- Добавлен `.nojekyll`

## 7. Attempts & Failures (ВАЖНО)

- **Попытка 1 (до def9a5d): Multipart-загрузка через `disk.file.upload`**
  - Что пробовали: `FormData` + `fetch` → `disk.file.upload`
  - Что НЕ сработало: HTTP 400 для файлов `.txt`, `.xlsx`, `.epf`
  - Почему: Bitrix24 вебхук не корректно обрабатывает multipart для некоторых MIME-типов

- **Попытка 2 (def9a5d): Base64 JSON через `disk.file.upload`**
  - Что пробовали: Кодирование файла в base64, отправка как JSON
  - Что НЕ сработало: Всё ещё HTTP 400 для части файлов

- **Попытка 3 (9c81ad6): `disk.file.upload` + версия badge**
  - Добавлен version badge v3 для отслеживания обновлений
  - Суть та же — base64 upload, не решено

- **Попытка 4 (32eeb78): 3 метода загрузки с автофоллбэком**
  - Что пробовали: raw binary / XHR+FormData / fetch+FormData — первый успешный побеждает
  - Частично сработало, но raw binary «сжигал» upload URL токен

- **Попытка 5 (v7.7): Удаление raw binary метода** ✅
  - Raw binary убран — он вызывал `DISK_BASE_SERVICE_22001` и сжигал токен
  - Оставлен только FormData — работает надёжно

- **Попытка 6: Наблюдатель через `AUDITORS` в `tasks.task.add`**
  - Что НЕ сработало: Поле `AUDITORS` игнорируется при создании задачи
  - Решение: Отдельный запрос после создания с перебором вариантов

- **Попытка 7: EOD через AUTHOR_ID**
  - Что пробовали: `AUTHOR_ID: '154'` в `task.commentitem.add`
  - Что НЕ сработало: Вебхук создан от пользователя 116 — Bitrix24 игнорирует AUTHOR_ID, подставляет владельца вебхука
  - Решение: Отдельный вебхук от бота (154)

- **Попытка 8: Создание пользователя бота через API**
  - `user.add` → ошибка `no_extranet_field` — нужен `UF_DEPARTMENT`
  - С `UF_DEPARTMENT: [1]` — создался пользователь ID 154
  - Email подтверждён через `notion@1c-cms.ru`
  - Вебхук создан вручную через UI (вход под ботом)

- **Попытка 9: Случайная модификация реального пользователя**
  - Пользователь 114 (Владимир Макаров) был переименован в «EOD Bot» через `user.update`
  - Это был реальный человек, не пустой пользователь
  - Имя восстановлено обратно через `user.update`

- **Попытка 10: GitHub Pages не деплоился (v7.8–v7.9)**
  - Причина 1: Битый submodule `bitrix-form` без `.gitmodules` — ломал `git submodule update` в Actions
  - Причина 2: Мусорные `.json` файлы с chmod 755 ломали Jekyll processing
  - Фикс: Удалён submodule, удалены .json файлы, добавлен `.nojekyll`

## 8. Bitrix24 API Notes

### Два вебхука
| Вебхук | URL | Назначение |
|--------|-----|------------|
| Пользователь (116) | `/rest/116/48yuunr8ss2u18qm/` | Создание задач, файлы, наблюдатели |
| EOD Bot (154) | `/rest/154/f896em13hhazm006/` | Только EOD-комментарии |

### Важные находки по API
- **AUTHOR_ID** в `task.commentitem.add` — **игнорируется** для вебхуков. Комментарий всегда от владельца вебхука
- **user.add** — работает в облачном Bitrix24, но требует `UF_DEPARTMENT` (иначе `no_extranet_field`)
- **Наблюдатель:** `task.observers.add` и `tasks.task.observers.add` возвращают 404. Рабочий метод: `tasks.task.update` с `AUDITORS: [userId]`
- **disk.folder.uploadfile:** Raw binary POST «сжигает» upload URL токен. Только FormData работает надёжно
- **BBCode:** Bitrix24 комментарии используют BBCode (`[b]`, `[i]`), НЕ HTML
- **Бот должен быть наблюдателем:** Без этого `task.commentitem.add` от хука бота вернёт «Недостаточно прав»

### Бот «Айти Лаб Bot» (ID 154)
- Email: `notion@1c-cms.ru`
- Пароль: `EodBot2026!xQ7`
- Позиция: «Системный бот»
- Отдел: ID 1
- Создан через `user.add` API с `UF_DEPARTMENT: [1]`

## 9. Decisions & Reasoning

- **Vanilla JS без фреймворков:** Форма — единственная страница, состояние минимально. Нулевой бандл, мгновенная загрузка
- **Один HTML-файл:** Удобно деплоить — загрузил один файл на GitHub Pages. Нет сборки, нет зависимостей
- **Два вебхука:** EOD должен приходить от бота, а не от пользователя. Bitrix24 привязывает комментарии к владельцу вебхука, AUTHOR_ID не работает. Единственное решение — отдельный вебхук от бота
- **Бот как наблюдатель:** Bitrix24 не позволяет комментировать чужие задачи без участия. Бот добавляется наблюдателем автоматически перед EOD-комментарием
- **BBCode вместо HTML:** Bitrix24 комментарии рендерят BBCode, HTML-теги отображаются как текст
- **FormData только для файлов:** Raw binary «сжигал» upload URL токен. После ошибки токен нельзя переиспользовать
- **Дедлайн локальное время:** `toISOString()` даёт UTC — может быть другой день. Используем `getFullYear/getMonth/getDate`
- **Форма сбрасывается при перезагрузке:** loadDraft() закомментирован — форма всегда чистая. Черновик сохраняется только для beforeunload предупреждения
- **Скролл-лок через position:fixed:** Единственный надёжный способ предотвратить прыжок на мобильных при autoGrow текстареей
- **Жёстко закодированные ID:** Команда и проекты стабильны. Нет смысла тянуть справочники из API
- **Хук бота захардкожен:** Не настраивается через UI — проще и безопаснее. При перевыпуске — правка одной строки в коде
- **Структурированное описание:** Задачи в команде были неструктурированными. Форма заставляет заполнить цель, что сделать, критерии приёмки

## 10. Limitations / Tech Debt

- **Мёртвый код:** Переменная `tags`, функции `addTag()`, `removeTag()`, `rT()` — UI удалён, код остался. В payload: `TAGS:tags` (пустой массив)
- **Один монолитный файл:** ~1569 строк HTML+CSS+JS. Нет модульности, нет тестов
- **Минифицированные имена функций:** `ag()`, `ufi()`, `up()`, `sC()`, `sP()`, `rL()`, `hS()`, `hF()`, `rF()`, `bD()`, `oS()`, `cS()`, `mWS()`, `sWH()`, `uRB()`, `fSz()`, `tF()`, `esc()`, `showT()`. Нечитаемо без документации
- **Нет unit-тестов:** Нет тестирования, ручная проверка
- **Нет CI/CD:** Коммиты напрямую в main, GitHub Pages Auto-deploy
- **CORS-зависимость:** Работает только через HTTP-сервер, не из `file://`
- **Файлы на диск с folder_id=3:** Жёстко закодированный ID папки. При изменении структуры диска — сломается
- **EOD зависит от addObserver:** Если бот не станет наблюдателем — EOD не сможет комментировать. Нет обработки этой ошибки отдельно
- **Хук бота не настраивается:** Захардкожен в коде, не через UI

## 11. Next Steps

- Удалить мёртвый код: `tags`, `addTag()`, `removeTag()`, `rT()`, `TAGS` из payload
- Раскодировать имена функций (`ag` → `autoGrow`, `hS` → `handleSubmit`, и т.д.)
- Вынести JS в отдельный файл для читаемости
- Сделать folder_id настраиваемым (не хардкод `id=3`)
- Добавить обработку ошибок при загрузке файлов с retry
- Оставить один рабочий вариант добавления наблюдателя (`tasks.task.update` с AUDITORS), убрать шум в консоли от 404
- Сделать хук бота настраиваемым через UI (или хотя бы в настройках)
- Добавить проверку валидности вебхука при сохранении (тестовый запрос к `profile`)
- Добавить возможность редактирования созданной задачи
- Обработка ошибки «бот не стал наблюдателем» — повторная попытка или toast-предупреждение

## 12. Quick Context for AI

### Как писать код в этом проекте
- Всё в одном файле `index.html`: CSS в `<style>`, JS в `<script>`, HTML в `<body>`
- Нет сборки, нет npm, нет бандлера. Прямой редактируемый HTML
- Деплой через push в main → GitHub Pages Auto-deploy
- Не добавляй внешние зависимости без крайней необходимости

### Принципы
- Минимализм: минимум зависимостей, нулевой бандл
- Практичность над красотой: форма должна работать надёжно
- Фоллбэки: если что-то может не работать — предусмотрены варианты
- Bitrix24 API нестабилен: всегда перебирай несколько вариантов эндпоинта
- BBCode для Bitrix24 комментариев, НЕ HTML

### Стиль кода
- CSS: Custom Properties (`--accent`, `--bg`, и т.д.), краткие имена классов, glass-морфизм
- JS: ES6+, async/await, краткие/минифицированные имена функций (исторически сложилось)
- HTML: Semantic5 где возможно, но в основном div-ная вёрстка с классами
- Форматирование: компактное, без лишних пробелов (исторически)
- Язык UI и комментариев: русский

### Ограничения
- Bitrix24 REST API через входящий вебхук — ограниченный набор методов, нет WebSocket, нет подписок
- Файлы загружаются только через `disk.folder.uploadfile` в папку id=3, затем привязываются к задаче
- Наблюдатель — только через `tasks.task.update` с AUDITORS после создания задачи
- Комментарии от бота — только если бот является наблюдателем задачи
- AUTHOR_ID не работает для вебхуков — комментарии всегда от владельца вебхука
- Мобильная версия: sidebar скрыт, сетка в одну колонку

### Ключевые аккаунты
| Объект | ID | Описание |
|--------|-----|----------|
| АндрейПредеин (user) | 116 | Владелец основного вебхука, менеджер проектов |
| АйтиЛаб Bot | 154 | EOD-бот, отдельный вебхук для комментариев |
| Папка диска | 3 | `disk.folder.uploadfile?id=3` — загрузка файлов |
| Проект Dakota | 32 | Единственный проект с дополнительной EOD-строкой |
